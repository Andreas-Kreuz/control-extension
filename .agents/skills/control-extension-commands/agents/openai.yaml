interface:
  display_name: "Control Extension Commands"
  short_description: "Approval workflow for sandbox-sensitive project commands"
  default_prompt: "Use $control-extension-commands before running any yarn command that may include web app tests/checks, including yarn check, yarn check:app, yarn check:web, yarn test:app, Cypress, Jekyll, or release builds in control-extension."
