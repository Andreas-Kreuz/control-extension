// App contract populated by:
// apps/web-server/src/server/mod/version/VersionSelector.ts
export interface VersionAppDto {
  id: string;
  name: string;
  eepVersion: string;
  luaVersion: string;
  singleVersion: string;
}
